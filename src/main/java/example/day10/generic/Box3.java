package example.day10.generic;

public class Box3 {
    // String, int 자료를 저장할 수 있는 자료형
    Object content;
    // Object는 모든 클래스의 최상위 클래스이므로 모든 클래스는 Object 형변환 할 수 있다.

    // 상속: extends
    // 상위 클래스로부터 멤버변수/메소드 물려받는 행위
    // 자식 타입/클래스는 부모 타입/클래스로 형변환 가능
    // 부모 타입/클래스는 자식 타입/클래스로 조건부 형변환
    // 1. 자료의 태생이 자식타입이어야 한다.
    // 2. (자식타입)부모타입
}
